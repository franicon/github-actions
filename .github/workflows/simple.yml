name: Shell Commands

on: push

jobs:
  run shell command:
    runs-on: ubuntu-alpine
    steps:
      - name: echo string
        run: echo "Hello world"
      - name: echo multi-line script
        run: |
          node -v
          npm -v
